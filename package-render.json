{
  "name": "mobycomps",
  "version": "1.0.0",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "install": "npm install -g vite typescript",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && NODE_ENV=production node server.js"
  },
  "dependencies": {
    "vite": "^5.4.14",
    "@vitejs/plugin-react": "^4.3.2",
    "tsx": "^4.19.1",
    "typescript": "5.6.3"
  }
}